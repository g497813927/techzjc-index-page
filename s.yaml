edition: 3.0.0
name: Alibaba Cloud FC Deployment for Index Site
access: "default"
vars:
  region: ${env:REGION}
resources:
  index-site:
    component: fc3
    props:
      functionName: ${env:FC_NAME}
      runtime: "custom-container"
      image: ${env:IMAGE}
      region: ${env:REGION}
